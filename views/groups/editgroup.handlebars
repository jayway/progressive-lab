<h1>{{group.name}}</h1>

<h2>Members</h2>
<ul>
 {{#each group.members}}
 <li>{{this}}</li>
 {{/each}}
</ul>

<form method="POST" action="/groups/edit/{{group.id}}">
  <select name="name">
    {{#each users}}
    <option value="{{this}}">{{this}}</option>
    {{/each}}
  </select>
  <button type="submit">Add</button>
</form>

<div>
<a href="/groups/adduser">Add new user</a>
